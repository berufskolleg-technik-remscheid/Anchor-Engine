package btr.engine.anchor.lwjgl.render.callbacks;

import btr.engine.anchor.lwjgl.LWJGLRenderLayer;
import org.lwjgl.glfw.GLFWScrollCallback;

public class ScrollCallback extends GLFWScrollCallback {

    public ScrollCallback(LWJGLRenderLayer renderLayer) {
    }

    @Override
    public void invoke(long window, double v, double v1) {

    }
}
